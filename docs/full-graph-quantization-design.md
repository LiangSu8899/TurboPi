# PI0 全图量化与TRT优化设计方案

## 1. 当前性能基线

### 1.1 MLP层性能测试结果 (18层, batch=1)

| Method | 总时间 | Per-Layer | vs TRT FP8 | 备注 |
|--------|--------|-----------|------------|------|
| BF16 (PyTorch) | 21.78 ms | 1.210 ms | 1.76x ↓ | 基准 |
| W4A16 PyTorch | 21.22 ms | 1.179 ms | 1.71x ↓ | 反量化+matmul |
| **W4A16 TVM (Python)** | **14.54 ms** | **0.808 ms** | **1.17x ↓** | DLPack接口 |
| **W4A16 TVM (Direct)** | **14.06 ms** | **0.781 ms** | **1.13x ↓** | 纯TVM |
| TRT FP8 | 12.39 ms | 0.688 ms | 1.00x | 目标 |

### 1.2 LIBERO任务精度验证

| 指标 | W4A16 TVM | BF16 基准 |
|------|-----------|-----------|
| 成功率 | 100% (10/10) | 100% |
| 平均延迟 | 174.6 ms | ~180 ms |
| 吞吐量 | 5.73 Hz | ~5.6 Hz |

**结论**: W4A16量化精度完全保持，MLP层比PyTorch BF16快1.55x

### 1.3 全模型延迟分布估算

```
┌─────────────────────────────────────────────────────────────────┐
│                PI0 推理延迟分布 (单次diffusion step)             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Vision Encoder (SigLIP):     ~15 ms  ████████████              │
│  PaliGemma Attention (18L):   ~11 ms  ███████                   │
│  PaliGemma MLP (18L):         ~14 ms  █████████  <- W4A16 TVM   │
│  Action Expert Attention:     ~2 ms   █                         │
│  Action Expert MLP:           ~4 ms   ██                        │
│  Diffusion Overhead:          ~5 ms   ███                       │
│                                                                  │
│  Total (estimated):           ~51 ms per step                    │
│  3 Steps:                     ~153 ms                            │
│  Actual Measured:             ~175 ms (额外开销)                 │
│                                                                  │
│  TRT FP8 Target:              ~40 ms per step = 120 ms total    │
├─────────────────────────────────────────────────────────────────┤
│  Gap to Target: ~55 ms (45%)                                    │
└─────────────────────────────────────────────────────────────────┘
```

## 2. 组件量化策略

### 2.1 量化方案对比

| 组件 | 当前状态 | 推荐方案 | 预期收益 |
|------|---------|---------|---------|
| Vision Encoder | PyTorch BF16 | TRT FP16 | 15ms → 8ms (-7ms) |
| PaliGemma Attention | PyTorch BF16 | TRT FP8 + FlashAttn | 11ms → 6ms (-5ms) |
| PaliGemma MLP | W4A16 TVM | W4A16 TRT Plugin | 14ms → 12ms (-2ms) |
| Action Expert Attn | PyTorch BF16 | TRT FP8 | 2ms → 1ms (-1ms) |
| Action Expert MLP | PyTorch BF16 | W4A16 TVM | 4ms → 3ms (-1ms) |
| Diffusion | PyTorch | CUDA Graph | 5ms → 2ms (-3ms) |

**总预期收益**: 51ms → 32ms (-19ms) = 96ms for 3 steps

### 2.2 各组件量化精度分析

```
精度敏感度 (高 → 低):
┌──────────────────────────────────────────────────────────────────┐
│                                                                   │
│  高精度区 (FP16/FP32):                                           │
│  ├── Diffusion Head (噪声预测关键)                               │
│  ├── Final LayerNorm                                              │
│  └── Action Output Projection                                     │
│                                                                   │
│  中精度区 (FP8):                                                  │
│  ├── Vision Encoder (SigLIP)                                     │
│  ├── Self-Attention Q/K/V Projection                             │
│  └── Cross-Attention                                              │
│                                                                   │
│  低精度区 (W4A16/INT4):                                          │
│  ├── MLP gate_proj / up_proj                                     │
│  ├── MLP down_proj                                                │
│  └── Embedding Layers (可选)                                      │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

## 3. TRT优化实施路线

### Phase 1: W4A16 TRT Plugin完善 (1-2天)

**目标**: 修复C++ TRT Plugin，消除Python开销

**当前问题**:
- `supportsFormatCombination` 返回false
- 需要支持TRT常量权重输入

**修复方案**:
```cpp
bool W4A16MLPPlugin::supportsFormatCombination(
    int32_t pos, DynamicPluginTensorDesc const* inOut,
    int32_t nbInputs, int32_t nbOutputs) noexcept
{
    // Input x: FP32 LINEAR
    if (pos == 0) {
        return inOut[pos].desc.type == DataType::kFLOAT &&
               inOut[pos].desc.format == TensorFormat::kLINEAR;
    }
    // Packed weights (INT8 interpreted as uint8)
    if (pos == 1 || pos == 3 || pos == 5) {
        return inOut[pos].desc.type == DataType::kINT8;
    }
    // Scales: FP32
    if (pos == 2 || pos == 4 || pos == 6) {
        return inOut[pos].desc.type == DataType::kFLOAT;
    }
    // Output: FP32
    if (pos == 7) {
        return inOut[pos].desc.type == DataType::kFLOAT;
    }
    return false;
}
```

**预期收益**: 14.06ms → 12.5ms (1.5ms/18层)

### Phase 2: Vision Encoder TRT导出 (2-3天)

**目标**: SigLIP视觉编码器TRT FP16优化

**步骤**:
1. 导出SigLIP到ONNX
2. TRT builder优化 (FP16)
3. 测试INT8 PTQ可行性

```python
# 导出示例
torch.onnx.export(
    siglip_model,
    (dummy_image,),
    "siglip.onnx",
    input_names=["image"],
    output_names=["features"],
    dynamic_axes={"image": {0: "batch"}},
    opset_version=17,
)

# TRT构建
trt_engine = trt.Builder(logger).build_serialized_network(
    network, config
)
```

**预期收益**: 15ms → 8ms (-7ms)

### Phase 3: Attention TRT FP8 (3-5天)

**目标**: 使用TRT原生FP8 Attention

**方案选择**:

| 方案 | 优点 | 缺点 |
|------|------|------|
| TRT FP8 GEMM | 原生支持 | 需要手动实现attention |
| FlashAttention-2 TRT | 高效融合 | 需要TRT 10.x |
| cuDNN FP8 MHA | 官方优化 | Thor兼容性待验证 |

**推荐**: TRT 10.x + FlashAttention-2 Plugin

```cpp
// TRT FP8 Attention Plugin
class FP8AttentionPlugin : public IPluginV3 {
    void enqueue(...) override {
        // 使用cuDNN FP8 MHA或FlashAttn
        cudnnMultiHeadAttnForward(
            handle, attnDesc,
            q_fp8, k_fp8, v_fp8,
            output, ...
        );
    }
};
```

**预期收益**: 11ms → 6ms (-5ms)

### Phase 4: 静态图编译与CUDA Graph (2-3天)

**目标**: 全图TRT + CUDA Graph消除kernel launch开销

**架构设计**:

```
┌─────────────────────────────────────────────────────────────────┐
│                    TRT Unified Engine                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐     ┌───────────────────────────────────────┐ │
│  │   Vision     │ ──> │         PaliGemma (18 layers)         │ │
│  │   TRT FP16   │     │  ┌─────────┐    ┌─────────────────┐   │ │
│  └──────────────┘     │  │Attn FP8 │ -> │ MLP W4A16 Plugin│   │ │
│                       │  └─────────┘    └─────────────────┘   │ │
│                       └───────────────────────────────────────┘ │
│                                      │                          │
│                                      v                          │
│                       ┌───────────────────────────────────────┐ │
│                       │      Action Expert (6 layers)         │ │
│                       │  ┌─────────┐ ┌──────────┐ ┌────────┐  │ │
│                       │  │Self-Attn│ │Cross-Attn│ │MLP W4A16  │ │
│                       │  │  FP8    │ │   FP8    │ │        │  │ │
│                       │  └─────────┘ └──────────┘ └────────┘  │ │
│                       └───────────────────────────────────────┘ │
│                                      │                          │
│                                      v                          │
│                       ┌──────────────────────────┐              │
│                       │   Diffusion Head FP16    │              │
│                       └──────────────────────────┘              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               v
                    ┌────────────────────┐
                    │   CUDA Graph       │
                    │   (3 steps)        │
                    │   消除launch开销   │
                    └────────────────────┘
```

**CUDA Graph实现**:
```python
# 捕获CUDA Graph
with torch.cuda.graph(graph):
    for step in range(3):
        out = trt_engine.execute(inputs)
        inputs = noise_scheduler.step(out, step)

# 执行
graph.replay()  # 极低overhead
```

**预期收益**: 5ms → 2ms (-3ms) 在diffusion overhead

## 4. 内存优化

### 4.1 权重内存对比

| 组件 | FP16 | W4A16 nvFP4 | 压缩比 |
|------|------|-------------|--------|
| PaliGemma MLP (18L) | 3456 MB | 1080 MB | 3.2x |
| Action Expert MLP (6L) | ~350 MB | ~110 MB | 3.2x |
| Attention Proj | ~500 MB | - (保持FP8) | - |
| Vision Encoder | ~400 MB | ~400 MB (FP16) | 1x |
| **总计** | ~4.7 GB | ~2.0 GB | **2.4x** |

### 4.2 KV Cache优化

```
KV Cache 量化方案:
┌────────────────────────────────────────────────────────────────┐
│                                                                 │
│  FP16 KV Cache:  968 tokens × 18 layers × 2048 × 2 = 68 MB     │
│  FP8 KV Cache:   968 tokens × 18 layers × 2048 × 2 = 34 MB     │
│                                                                 │
│  建议: 使用FP8 KV Cache (精度影响小，内存减半)                  │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

## 5. 实施计划

### 时间线

```
Week 1:
├── Day 1-2: 修复W4A16 TRT Plugin format combination
├── Day 3-4: 集成到完整推理pipeline
└── Day 5: 验证精度和性能

Week 2:
├── Day 1-3: Vision Encoder TRT FP16导出
├── Day 4-5: Attention TRT FP8 原型
└── Day 6-7: 集成测试

Week 3:
├── Day 1-3: 全图TRT编译
├── Day 4-5: CUDA Graph优化
└── Day 6-7: 最终benchmark和文档
```

### 里程碑

| 阶段 | 目标延迟 | 吞吐量 | 状态 |
|------|---------|--------|------|
| 当前 (W4A16 TVM) | 174.6 ms | 5.7 Hz | ✅ 完成 |
| Phase 1 (TRT Plugin) | 160 ms | 6.3 Hz | 🔄 进行中 |
| Phase 2 (Vision TRT) | 140 ms | 7.1 Hz | ⏳ 待开始 |
| Phase 3 (Attn FP8) | 115 ms | 8.7 Hz | ⏳ 待开始 |
| Phase 4 (CUDA Graph) | **100 ms** | **10 Hz** | ⏳ 待开始 |

## 6. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| Thor SM110 TRT FP8支持不完整 | 高 | 回退到FP16 + INT8混合 |
| W4A16精度在某些任务下下降 | 中 | 保留BF16回退路径 |
| CUDA Graph与动态shape不兼容 | 中 | 固定sequence length |
| TRT 10.x在Jetson上不可用 | 低 | 使用TRT 9.x兼容方案 |

## 7. 下一步行动

1. **立即**: 修复W4A16 TRT Plugin的`supportsFormatCombination`
2. **本周**: 完成TRT Plugin集成到推理pipeline
3. **下周**: 开始Vision Encoder TRT导出

---

*文档版本: 2026-02-11*
*作者: Claude Code*
